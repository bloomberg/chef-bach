# Metrics schema
 
# --- !Ups

ALTER TABLE metric MODIFY COLUMN target_desc VARCHAR(1000) DEFAULT '-unkown-';

# --- !Downs

ALTER TABLE metric MODIFY COLUMN target_desc VARCHAR(255) DEFAULT '-unkown-';
