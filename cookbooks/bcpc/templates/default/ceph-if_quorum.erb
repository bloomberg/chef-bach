#!/bin/sh
################################################
#
#              Generated by Chef
#
################################################

if ceph --admin-daemon=/var/run/ceph/ceph-mon.<%="#{node.hostname}"%>.asok mon_status | grep state | grep -e 'leader\|peon' > /dev/null; then
    $*
fi
