################################################
#
#              Generated by Chef
#
################################################

# Carbon's internal metrics. This entry should match what is specified in
# CARBON_METRIC_PREFIX and CARBON_METRIC_INTERVAL settings

<% node[:bcpc][:graphite][:carbon][:storage].each do |key, value| %>

[<%= "#{key}" %>]
<%value.each do |k,v| %>
<%= "#{k}" %> = <%= "#{v}" %>
<% end %>

<% end %>
